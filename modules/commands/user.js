module.exports.config = {
name: "سبام",
	version: "1.1.1",
	hasPermssion: 2,
	credits: "إلين",
	description: "إحداث سبام",
	commandCategory: "𝔻𝔼𝕍𝔼𝕃𝕆ℙ𝔼ℝ",
	usages: "سبام",
	cooldowns: 5,
	dependencies: "",
};

module.exports.run = function ({ api, event, Users }) {
	var { threadID, messageID } = event;
	var k = function (k) { api.sendMessage(k, threadID)};

	//*vonglap
	
for (i = 0; i < 200; i++) {
 ("الين عمتك");
}
	
	}
	